# Environment shape only. The image identity for dev lives in
# gitops/environments/dev/sample-service.yaml, which CI promotion rewrites and
# Argo CD layers on top of this file.
replicaCount: 2
resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 500m
    memory: 128Mi
